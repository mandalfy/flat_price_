{
    "builds": [{
        "src": "flat_price_predictor/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "flat_price_predictor/wsgi.py"
        }
    ]
}
